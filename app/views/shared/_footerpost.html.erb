	                    <dl class="dl-horizontal">
	                        <dt><%= image_tag (post.picture.thumb.url) if post.picture? %></dt>
	                        <dd>
	                            <%= link_to (truncate( raw( sanitize(post.text, :tags => %w(table tr td), :attributes => %w(id class style) ) ), :length => 165, :omission => "...")), post, :controller => :posts, :action => :show %>
	                        </dd>
	                    </dl><br>