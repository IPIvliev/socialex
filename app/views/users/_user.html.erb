        <div class='users'>

          <div class='profile_image'>
            <%= link_to (image_tag 'https://graph.facebook.com/'+user.uid+'/picture?type=normal&width=86&height=86'), user, :class => "img-thumbnail" %>
          </div>
          <div class='profile_name'>
            <div class="price">
              <%= stock_price(user.id) %> (<%= compare(user) %>)
            </div>
            <%= user.name %> 
          </div>
        </div>