<div class="container top-30">
	<div class="row">
		<div class="col-md-8">
			<h3>Пополнить счёт</h3>
    <%= payment_service_for 21, "Socialex",
                          :amount => 300,
                          :service => :robokassa,
                          :secret => "OdergiesSE1" do |s| %>
    <%= submit_tag "Внести взнос", :class => "btn" %>
    <% end %>
		</div>
		<%= render 'shared/column' %>
	</div>
</div>